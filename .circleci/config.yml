version: 2.1
jobs:
  build:
    docker:
      - image: node:9.9.0
    working_directory: ~/code-trails-blog
    steps:
      - checkout
      - run:
          name: Install Hexo CLI
          command: npm install -g hexo-cli
      - restore_cache:
          keys:
            - npm-deps-{{ checksum "package.json" }}
      - run: